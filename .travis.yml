# https://travis-ci.org/tornadoweb/tornado
language: python
python:
    - 2.7.8
    - 2.7
    - 3.3
    - 3.4
    - 3.5
    - 3.6
    - nightly

install:
    - pip install -U tox

script:
    - tox
    - tox -e flake8

sudo: false

matrix:
    fast_finish: true
